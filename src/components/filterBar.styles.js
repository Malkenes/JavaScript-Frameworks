import styled from "styled-components";

export const StyledFilterBar = styled.div`
    display: flex;
    gap: 1em;
    margin-bottom: 20px;
    button {
        background: #FDEEED;
        padding: 12px 8px;
        text-wrap: nowrap;
    }
`